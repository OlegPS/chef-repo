[proc]
user root
<% node["coollan"]["munin-node"]["plugin"]["proc"].each do | key, value | %>
<%= key %> <%= value %>
<% end %>
env.procaspect cpu|memory|processes
env.category Processes
#env.procargs
#env.procuser
